[package]
name = "ubar-xpi-verifier"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[dependencies]
openssl = { version = "0.10", features = ["vendored"] }
serde_json = "1"
ubar-extension-host = { path = "../extension-host" }
