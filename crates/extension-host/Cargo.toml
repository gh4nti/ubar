[package]
name = "ubar-extension-host"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[dependencies]
getrandom = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
zip = { version = "4", default-features = false, features = ["deflate"] }
