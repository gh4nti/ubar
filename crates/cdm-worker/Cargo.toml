[package]
name = "ubar-cdm-worker"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[dependencies]
libloading = "0.8"
serde_json = "1"
ubar-drm-host = { path = "../drm-host" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
