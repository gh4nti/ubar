[package]
name = "ubar-crx3-verifier"
version = "0.1.0"
edition = "2024"
license = "BSD-3-Clause"

[dependencies]
openssl = { version = "0.10", features = ["vendored"] }
serde_json = "1"
sha2 = "0.10"
ubar-extension-host = { path = "../extension-host" }
