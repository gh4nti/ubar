[package]
name = "ubar-browser-core"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ubar-engine-abi = { path = "../engine-abi" }
