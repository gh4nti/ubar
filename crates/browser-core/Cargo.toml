[package]
name = "ubar-browser-core"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
ubar-engine-abi = { path = "../engine-abi" }
